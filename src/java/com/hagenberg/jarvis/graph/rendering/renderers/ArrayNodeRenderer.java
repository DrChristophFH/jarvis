package com.hagenberg.jarvis.graph.rendering.renderers;

import com.hagenberg.imgui.Draw;
import com.hagenberg.jarvis.graph.LayoutableNode;

public class ArrayNodeRenderer implements NodeRenderer {
    @Override
    public void render(Draw draw, LayoutableNode node) {
        // default rendering for ArrayNode
    }
}

